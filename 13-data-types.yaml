- name: testing Datatype
  connection: local
  hosts: local
  vars:
    NAME: "Sumanth"
    AGE: 31
    LIVED_IN:
      - Gudivada
      - Bhimavaram
      - Manthena
      - Vijayawada
      - Hyderabad
    COLLEGES:
      10th: "Montessori Telugu medium"
      Diploma: "VKR and VNB Polytechnic"
      Btech: BIET
  tasks:
  - name: Printing User information
    ansible.builtin.debug:
      msg: "user name is {{ NAME }} and Age is {{ AGE }} adn he lived in {{ LIVED_IN }} and colleges went to {{ COLLEGES }}" 

- name: DevOps course information
  hosts: local
  connection: local
  vars:
    course_name: "DevOps with AWS" # string
    duration: 120 #number
    topics: # list, [] this means list
    - linux
    - shell
    - ansible
    - AWS
    live: true # boolean
    tools: #map, {} this means map
      ci: jenkins
      cloud: aws
      containerization: docker
  tasks:
  - name: print course information
    ansible.builtin.debug:
      msg: "Hello I am learning {{ course_name }}, duration: {{ duration }}, topics are: {{ topics }}, is it live?: {{ live }}, tools covered are: {{ tools }}"